import React from "react";

function Post() {
  return (
    <div className="post">
      {/* avatar + username + moreHoriz*/}
      {/* image */}
      {/* like + comment + share + turnedIn */}
      {/* username + caption */}
    </div>
  );
}

export default Post;
